# Revenue AI Agent with Ollama and LangChain

An automated financial analysis agent that takes a natural language prompt,
resolves company names to stock tickers, runs a full fundamental analysis,
and produces charts, an interactive dashboard, and a written report — all locally,
with no external API keys required.

The orchestration pipeline is handled deterministically in Python. The LLM
(running locally via Ollama) is used for natural language understanding and
for writing the discursive report commentary.

---

## How it works

The agent receives a prompt like *"Analyse the top European banks"* or
*"Compare Apple, Microsoft and Nvidia"* and autonomously:

1. **Resolves tickers** — maps company names or sector descriptions to valid
   Yahoo Finance symbols using a built-in knowledge base
2. **Runs the analysis** — downloads financial statements, computes 17+
   indicators, calculates multi-year trends and CAGR
3. **Generates reports** — produces PNG charts, a PDF summary, an interactive
   Plotly HTML dashboard, and a written analyst commentary generated by the LLM

The orchestration pipeline is handled deterministically in Python. The LLM
(running locally via Ollama) is used for natural language understanding and
for writing the discursive report commentary.

---

## Project structure

```
Revenue-AI-Agent-with-Ollama-and-Langchain/
├── agent.py              # Main entry point — interactive loop
├── ticker_resolver.py    # Resolves natural language to valid tickers
├── tool_analysis.py      # Runs the full financial analysis pipeline
├── report_generator.py   # Generates HTML and PDF reports via LLM
└── requirements.txt
```

> This agent depends on [wheelhouse](https://github.com/YOUR_USERNAME/wheelhouse)
> as a local package for the core financial analysis logic
> (data fetching, indicator calculation, visualisations).

---

## Dependencies

### Core
- [Ollama](https://ollama.com) — local LLM runtime
- [LangChain](https://docs.langchain.com) — agent orchestration
- [LangGraph](https://langgraph.dev) — tool use framework
- [yfinance](https://github.com/ranaroussi/yfinance) — financial data
- [wheelhouse](https://github.com/YOUR_USERNAME/wheelhouse) — analysis core

### Python packages
```
langchain>=1.2.0
langchain-ollama>=0.3.0
langchain-core>=1.2.0
langgraph>=1.1.0
```

---

## Setup

### 1. Prerequisites
- Python 3.9+
- [Ollama](https://ollama.com) installed and running
- [wheelhouse](https://github.com/YOUR_USERNAME/wheelhouse) cloned and installed

### 2. Install wheelhouse as a local package
```bash
git clone https://github.com/YOUR_USERNAME/wheelhouse
cd wheelhouse
pip install -e .
```

### 3. Pull the LLM model
```bash
ollama pull qwen2.5:3b
```

### 4. Clone this repo and install dependencies
```bash
git clone https://github.com/YOUR_USERNAME/Revenue-AI-Agent-with-Ollama-and-Langchain
cd Revenue-AI-Agent-with-Ollama-and-Langchain
pip install -r requirements.txt
```

### 5. Set the wheelhouse path
Open `agent.py` and update this line with your local path:
```python
WHEELHOUSE_PATH = r"C:/path/to/your/wheelhouse"
```

### 6. Run the agent
```bash
python agent.py
```

---

## 💬 Example prompts

```
You: Analyse the top European automotive OEMs
You: Compare BMW.DE and MBG.DE
You: Run a full analysis on European banks
You: Analyse Apple, Microsoft and Nvidia
You: banche europee
You: big tech us
```

Ticker symbols can also be provided directly:
```
You: UCG.MI, ISP.MI, BNP.PA
```

---

## Output

Each analysis run creates a dedicated folder inside `output_agent/`:

```
output_agent/
└── Apple_Microsoft_NVIDIA/
    ├── report.html                   ← full report with charts embedded
    ├── report.pdf                    ← discursive analyst commentary
    ├── trend_dashboard.html          ← interactive Plotly dashboard
    ├── automotive_analysis.xlsx      ← raw data and indicators (Excel)
    ├── automotive_analysis_charts.pdf← all charts in one PDF
    └── charts/
        ├── bar_*.png                 ← per-indicator comparison charts
        ├── trend_*.png               ← multi-year trend line charts
        ├── cagr_heatmap.png          ← CAGR heatmap
        └── market_*.png              ← market multiples charts
```

---

## Financial indicators computed

| Category | Indicators |
|---|---|
| **Absolute** | Revenue, Gross Profit, EBIT, EBITDA, Net Income, Net Debt |
| **Margins** | Gross Margin, EBIT Margin, EBITDA Margin, Net Margin |
| **Returns** | ROE, ROA, ROI / ROCE |
| **Liquidity** | Current Ratio, Quick Ratio |
| **Structure** | Debt/Equity, Interest Coverage |
| **Market** | P/E, EV/EBITDA, Price/Book, Market Cap, Beta, Dividend Yield |
| **Growth** | CAGR for key indicators over available history |

---

## Sector presets

The agent recognises the following sector keywords out of the box:

| Keyword | Companies |
|---|---|
| `automotive europeo` | VW, Stellantis, Mercedes, BMW, Renault, Porsche, Volvo Cars, TRATON, Iveco |
| `banche europee` | UniCredit, Intesa, BNP, Santander, Deutsche Bank, HSBC, Barclays, SocGen, ING |
| `big tech us` | Apple, Microsoft, Alphabet, Amazon, Meta, Nvidia |

New sectors can be added in `ticker_resolver.py → SECTOR_PRESETS`.

---

## 🤖 LLM model

The agent uses `qwen2.5:3b` by default — a compact, capable model that runs
comfortably on a machine with 8 GB RAM. The model is used for:
- Parsing natural language prompts
- Writing the discursive analyst commentary in `report.html` and `report.pdf`

To switch model, update this line in `agent.py` and `report_generator.py`:
```python
ChatOllama(model="qwen2.5:3b", ...)
```

Tested models: `qwen2.5:3b`, `qwen2.5:7b`, `llama3.2:3b`.

---

## Notes

- Data is sourced from Yahoo Finance via `yfinance` for **educational / personal use only**
- Absolute values (Revenue, EBIT, etc.) are in **millions** in the original reporting currency
- Data availability may vary by ticker and exchange
- The agent pipeline is deterministic — the LLM does not control tool sequencing

---

## Roadmap

- [ ] Streamlit web interface
- [ ] Quarterly data support
- [ ] Automatic sector detection from free-text prompt
- [ ] Expanded sector presets (energy, pharma, luxury)
- [ ] Report export in multiple languages
